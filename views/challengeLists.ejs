<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Challenges List</title>
    <link rel="stylesheet" href="/adminHeader.css" type="text/css" media="all" />

    <style>
        :root {
            --color-bg: #f6f8fa;
            --color-text: #24292e;
            --color-border: #e1e4e8;
            --color-btn-bg: #2ea44f;
            --color-btn-text: #ffffff;
            --color-btn-border: rgba(27,31,35,0.15);
            --color-btn-shadow: 0 1px 0 rgba(27,31,35,0.1);
            --color-btn-hover-bg: #2c974b;
            --color-link: #0366d6;
            --color-header-bg: #24292e;
            --color-header-text: #ffffff;
            --color-table-header-bg: #f1f8ff;
            --font-sans: -apple-system,BlinkMacSystemFont,"Segoe UI",Helvetica,Arial,sans-serif,"Apple Color Emoji","Segoe UI Emoji";
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: var(--font-sans);
            line-height: 1.5;
            color: var(--color-text);
            background-color: var(--color-bg);
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 1rem;
        }

        h1 {
            font-size: 24px;
            font-weight: 600;
            margin-bottom: 24px;
        }

        .challenges-table {
            width: 100%;
            border-collapse: collapse;
            background-color: #ffffff;
            border: 1px solid var(--color-border);
            border-radius: 6px;
            overflow: hidden;
        }

        .challenges-table th,
        .challenges-table td {
            padding: 12px 16px;
            text-align: left;
            border-bottom: 1px solid var(--color-border);
        }

        .challenges-table th {
            background-color: var(--color-table-header-bg);
            font-weight: 600;
        }

        .challenges-table tr:last-child td {
            border-bottom: none;
        }

        .challenges-table tr:hover {
            background-color: var(--color-bg);
        }

        .challenge-link {
            color: var(--color-link);
            text-decoration: none;
        }

        .challenge-link:hover {
            text-decoration: underline;
        }

        .status-badge {
            display: inline-block;
            padding: 2px 8px;
            border-radius: 12px;
            font-size: 12px;
            font-weight: 500;
        }

        .status-open {
            background-color: #28a745;
            color: #ffffff;
        }

        .status-pending {
            background-color: #ffd33d;
            color: #24292e;
        }

        .status-closed {
            background-color: #d73a49;
            color: #ffffff;
        }
        .content-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 2rem;
        }
        .btn {
            background-color: var(--color-btn-bg);
            color: #ffffff;
            padding: 0.5rem 1rem;
            border: none;
            border-radius: 6px;
            cursor: pointer;
            font-size: .8rem;
            font-weight: 500;
            transition: background-color 0.2s ease-in-out;
            text-decoration: none;
        }

        @media (max-width: 768px) {
            .challenges-table {
                font-size: 12px;
            }

            .challenges-table th,
            .challenges-table td {
                padding: 8px;
            }
        }

        @media (max-width: 480px) {
            
        }
    </style>
</head>
<body>
    <%- include("./partials/adminHeader.ejs") -%>
    <main class="container">
        <h1>Challenges List</h1>
        <div class="content-header">
         
          <a href="/admin/addChallenge" class="btn">Create New Challenge</a>
        </div>
        <table class="challenges-table">
            <thead>
                <tr>
                    <th>Challenge Name</th>
                    <th>Status</th>
                    <th>Deadline</th>
                    <th>Submissions</th>
                </tr>
            </thead>
            <tbody>
              <% challenges.forEach(challenge => { %>
                <tr>
                    <td><a href="/admin/challenges/<%=challenge.name%>" class="challenge-link" data-id="1"><%=challenge.name%></a></td>
                    <td><span class="status-badge status-open"><%=challenge.status%></span></td>
                    <td>2023-06-30</td>
                    <td>8</td>
                </tr>
           <%   }) %>
               
            </tbody>
        </table>
    </main>

    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const challengeLinks = document.querySelectorAll('.challenge-link');
            
            
            });
        });
    </script>
</body>
</html>